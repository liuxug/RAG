# 智识RAG文档问答中台 - 操作手册

## 一、系统概述

### 1.1 系统简介

**智识RAG文档问答中台**是基于检索增强生成（RAG）技术构建的企业级文档问答系统。系统支持上传多种格式文档，自动进行向量索引，通过语义检索与AI生成相结合的方式，为用户提供准确、可溯源的智能问答服务。

### 1.2 核心功能

| 模块 | 功能描述 |
|------|----------|
| 文档库管理 | 文档上传、分类、审核、发布、删除等全生命周期管理 |
| 智能检索 | 关键词匹配+向量检索混合搜索，支持历史记录和热门搜索 |
| AI对话 | 基于知识库的智能问答，支持多轮对话和模型切换 |
| 用户管理 | 用户创建、编辑、删除、禁用/启用，导入导出 |
| 角色管理 | 角色创建、权限配置、角色状态管理 |
| 全局统计 | 文档统计、用户统计、角色统计等数据可视化 |
| 文档分享 | 生成临时分享链接，支持外部访问 |

### 1.3 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (React)                        │
│  DocumentLibrary │ SearchPage │ ChatPage │ UserManagement   │
│  RoleManagement │ GlobalStats │ SharePage                   │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP/REST
┌──────────────────────────▼──────────────────────────────────┐
│                       API层 (FastAPI)                       │
│  Auth │ Routes │ Roles │ Stats                              │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      业务逻辑层                              │
│  Ingest │ Retrieval │ Generation                            │
│  (文档处理) │ (检索引擎) │ (答案生成)                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                       数据层                                 │
│  Chroma (向量库) │ JSON Files (业务数据)                    │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 支持的文档格式

- **PDF** - 支持通过PyMuPDF解析
- **Markdown** - `.md` 文件
- **TXT** - 纯文本文件
- **Word** - `.docx` 文件
- **Excel** - `.xlsx`, `.ods`, `.csv` 文件
- **ODT** - OpenDocument 文本文件

---

## 二、快速开始

### 2.1 环境要求

| 依赖 | 版本要求 | 说明 |
|------|----------|------|
| Python | 3.10+ | 后端运行环境 |
| Node.js | 18+ | 前端运行环境 |
| Ollama | 可选 | 本地LLM支持 |

### 2.2 后端启动

```bash
# 进入项目目录
cd rag

# 安装依赖
pip install -r requirements.txt

# 启动服务
python main.py
```

服务默认运行在 `http://localhost:8000`

### 2.3 前端启动

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端默认运行在 `http://localhost:5173`

### 2.4 配置说明

配置文件位于 `rag/.env`：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| LLM_TYPE | openai | LLM类型：ollama/openai/dashscope |
| OLLAMA_MODEL | qwen2.5:7b | 本地模型名称 |
| OPENAI_API_KEY | - | OpenAI API Key |
| OPENAI_MODEL | gpt-4o-mini | 云端模型名称 |
| OPENAI_BASE_URL | https://api.chatanywhere.tech | OpenAI中转地址 |
| DASHSCOPE_API_KEY | - | 通义千问API Key |
| EMBEDDING_MODEL | BAAI/bge-large-zh-v1.5 | 中文嵌入模型 |
| CHROMA_DB_PATH | ./chroma_db | 向量数据库路径 |
| UPLOAD_DIR | ./uploads | 文件上传目录 |
| CHUNK_SIZE | 800 | 文本分块大小 |
| CHUNK_OVERLAP | 150 | 分块重叠大小 |
| TOP_K | 5 | 检索返回数量 |
| PORT | 8000 | 服务端口 |

---

## 三、用户指南

### 3.1 登录与注册

#### 登录

1. 打开浏览器访问 `http://localhost:5173`
2. 进入登录页面，输入用户名和密码
3. 点击「登录」按钮

#### 注册

1. 在登录页面点击「注册」链接
2. 填写用户名、邮箱、密码
3. 获取验证码并填写
4. 点击「注册」按钮完成注册

### 3.2 角色与权限

系统预设以下角色：

#### 权限矩阵

| 权限ID | 权限名称 | 说明 |
|--------|----------|------|
| doc_view | 文档查看 | 查看文档列表和详情 |
| doc_upload | 文档上传 | 上传新文档 |
| doc_edit | 文档编辑 | 修改文档分类等信息 |
| doc_delete | 文档删除 | 删除文档 |
| doc_download | 文档下载 | 下载文档文件 |
| doc_review | 文档审核 | 审核文档（批准/驳回） |
| doc_comment | 文档评论 | 对文档发表评论 |
| search | 智能检索 | 使用智能检索功能 |
| chat | AI对话 | 使用AI对话功能 |
| knowledge_graph | 知识图谱 | 查看知识图谱 |
| recommend | 推荐文档 | 获取推荐文档 |
| stats_view | 统计查看 | 查看数据统计 |
| llm_switch | 模型切换 | 切换LLM模型类型 |
| user_view | 用户查看 | 查看用户列表 |
| user_create | 用户创建 | 创建新用户 |
| user_edit | 用户编辑 | 编辑用户信息 |
| user_delete | 用户删除 | 删除用户 |
| role_view | 角色查看 | 查看角色列表 |
| role_create | 角色创建 | 创建新角色 |
| role_edit | 角色编辑 | 编辑角色权限 |
| role_delete | 角色删除 | 删除角色 |
| role_disable | 角色禁用 | 禁用/启用角色 |
| history_view | 历史记录 | 查看搜索/下载历史 |
| history_manage | 历史管理 | 管理历史记录（删除） |

---

## 四、功能操作指南

### 4.1 文档库管理

#### 4.1.1 上传文档

**操作步骤：**

1. 进入「文档库」页面
2. 点击「上传文档」按钮
3. 在弹出的上传区域中：
   - 拖拽文件到上传区域，或点击选择文件
   - 选择文档分类（技术/人力资源/财务/法务/市场/产品/其他）
   - 设置初始状态（草稿/待审核）
4. 点击「确认上传」
5. 等待上传和索引完成，系统会显示处理进度

**注意事项：**
- 仅支持PDF、Markdown、TXT、Word、Excel等格式
- 文件大小建议不超过50MB
- 上传后文档会自动进行文本分块和向量索引

#### 4.1.2 文档状态流转

```
草稿 ──提交审核──→ 待审核 ──批准──→ 已发布
                    │            ↑
                    └──驳回──→ 审核驳回──重新提交──┘
                              │
                    任何状态 ──删除──→ 已删除 ──恢复──→ 草稿
```

#### 4.1.3 文档筛选与排序

**筛选选项：**
- **类型筛选**：全部、PDF、Word、Excel、Markdown、TXT、压缩包、其他
- **分类筛选**：全部、技术、人力资源、财务、法务、市场、产品、其他
- **状态筛选**：全部、草稿、待审核、已发布、审核驳回、已删除

**排序选项：**
- 名称升序/降序
- 页数升序/降序

#### 4.1.4 文档操作

| 操作 | 权限要求 | 说明 |
|------|----------|------|
| 查看详情 | doc_view | 查看文档详细信息 |
| 下载 | doc_download | 下载文档原文件 |
| 分享 | doc_view | 生成临时分享链接 |
| 修改分类 | doc_edit | 修改文档所属分类 |
| 提交审核 | doc_upload | 将草稿提交审核 |
| 审核批准 | doc_review | 批准待审核文档 |
| 审核驳回 | doc_review | 驳回待审核文档 |
| 删除 | doc_delete | 删除文档 |
| 恢复 | doc_upload | 恢复已删除文档 |

#### 4.1.5 文档审核

**审核操作流程：**

1. 在文档库中筛选「待审核」状态的文档
2. 点击文档名称进入详情页
3. 查看文档内容和基本信息
4. 点击「批准」或「驳回」按钮
5. 若驳回，需填写驳回理由
6. 确认操作，系统记录审核日志

---

### 4.2 智能检索

#### 4.2.1 基本搜索

1. 进入「智能检索」页面
2. 在搜索框中输入关键词
3. 系统自动进行搜索（支持500ms防抖）
4. 查看搜索结果列表

**搜索结果包含：**
- 文档标题
- 匹配率（关键词匹配+向量相似度）
- 文件类型
- 更新时间
- 作者
- 摘要预览

#### 4.2.2 搜索过滤

支持按文件类型过滤：
- 全部
- PDF
- Word
- Excel
- Markdown
- TXT

#### 4.2.3 搜索历史

**查看历史：**
- 页面右侧显示最近搜索记录
- 点击历史记录可快速重新搜索

**管理历史：**
- 点击「清空搜索历史」按钮
- 删除单个历史记录

#### 4.2.4 热门搜索

页面右侧显示热门搜索词，点击可快速搜索。

#### 4.2.5 相关文档推荐

搜索结果下方显示与当前搜索相关的文档推荐列表。

---

### 4.3 AI对话

#### 4.3.1 发起对话

1. 进入「AI对话」页面
2. 在输入框中输入问题
3. 点击发送按钮或按Enter键
4. 等待AI生成回答

**推荐问题示例：**
- 绩效考核的评分标准是什么？
- 年假政策有哪些变化？
- API接口的认证方式有几种？

#### 4.3.2 回答来源引用

AI回答会附带引用来源，格式如下：
```
【参考来源】
- 星辰科技企业员工手册（第2-5页）
- 2026年Q2季度财务报告（第12页）
```

#### 4.3.3 临时文档上传

在对话中可以上传临时文档：
1. 点击输入框旁的附件图标
2. 选择本地文件
3. 文件上传后自动加入当前对话的上下文
4. 对话结束后临时文档自动删除

#### 4.3.4 对话历史

1. 点击「历史记录」按钮
2. 查看历史对话列表
3. 点击可切换到历史对话
4. 支持删除单条对话或清空全部

#### 4.3.5 模型切换（权限：llm_switch）

1. 点击右上角设置图标
2. 选择模型类型：
   - **云端模型**（GPT-4o-mini）：响应快速，知识更新
   - **本地模型**（Qwen2.5）：数据不出网，安全可控
3. 调整参数：
   - **温度**（0-1）：越高越随机，越低越确定性
   - **最大Token**（2048/4096/8192）：回答长度限制

---

### 4.4 用户管理

#### 4.4.1 用户列表

1. 进入「用户管理」页面
2. 查看用户列表，包含：
   - 用户头像（首字母）
   - 用户名
   - 邮箱
   - 角色（带颜色标识）
   - 部门
   - 状态（启用/禁用）
   - 创建时间
   - 最后登录时间

#### 4.4.2 创建用户

1. 点击「新增用户」按钮
2. 填写用户信息：
   - 用户名
   - 邮箱
   - 密码
   - 角色
   - 部门
3. 点击「确定」保存

#### 4.4.3 编辑用户

1. 在用户列表中点击「编辑」按钮
2. 修改用户信息（用户名不可修改）
3. 点击「确定」保存

#### 4.4.4 删除用户

1. 在用户列表中点击「删除」按钮
2. 确认删除操作
3. 系统记录删除日志

#### 4.4.5 导入用户

1. 点击「导入用户」按钮
2. 选择CSV格式的用户文件
3. 系统批量导入用户

---

### 4.5 角色管理

#### 4.5.1 角色列表

1. 进入「角色管理」页面
2. 查看角色卡片列表，包含：
   - 角色名称
   - 角色描述
   - 权限数量
   - 成员数量
   - 状态（启用/禁用）

#### 4.5.2 创建角色

1. 点击「创建角色」按钮
2. 填写角色信息：
   - 角色名称
   - 角色描述
   - 选择权限（支持多选）
3. 点击「保存」按钮

#### 4.5.3 编辑角色

1. 在角色卡片中点击「编辑」按钮（仅非内置角色）
2. 修改角色名称、描述或权限
3. 点击「保存」按钮

**注意：** 内置角色（超级管理员）不可修改。

#### 4.5.4 查看权限

1. 在角色卡片中点击「查看权限」按钮
2. 查看该角色拥有的所有权限列表

#### 4.5.5 禁用/启用角色

1. 在角色卡片中点击「禁用」或「启用」按钮
2. 确认操作

**注意：** 内置角色不可禁用。

#### 4.5.6 删除角色

1. 在角色卡片中点击「删除」按钮
2. 确认删除操作

**注意：**
- 内置角色不可删除
- 有用户关联的角色不可删除

---

### 4.6 全局统计

1. 进入「全局统计」页面
2. 查看以下统计数据：
   - 文档统计：总文档数、已发布数、待审核数、本周新增
   - 用户统计：总用户数、活跃用户数、禁用用户数
   - 角色统计：总角色数、活跃角色数、内置角色数
   - 知识库统计：总片段数、总页数

---

### 4.7 文档分享

#### 生成分享链接

1. 在文档库或文档详情页点击「分享」按钮
2. 系统生成临时分享链接
3. 复制链接并发送给他人

**分享链接特性：**
- 有效期：24小时
- 无需登录即可访问
- 仅可查看文档内容，不可下载

---

## 五、文档状态说明

| 状态 | 图标颜色 | 说明 | 可执行操作 |
|------|----------|------|------------|
| 草稿 | 灰色 | 文档待完善，未提交审核 | 编辑、提交审核、删除 |
| 待审核 | 蓝色 | 文档已提交，等待审核 | 审核批准、审核驳回 |
| 已发布 | 绿色 | 文档审核通过，可被检索 | 下载、分享、评论、删除 |
| 审核驳回 | 红色 | 文档审核未通过 | 修改后重新提交、删除 |
| 已删除 | 灰色 | 文档已删除，可恢复 | 恢复、永久删除 |
| 临时 | 透明 | 对话上传的临时文档，自动删除 | 无（自动清理） |

---

## 六、搜索算法说明

系统采用**混合检索策略**，结合关键词匹配和向量检索：

### 6.1 关键词匹配优先级

1. **标题精确匹配** → 得分：1.0
2. **标题前缀匹配** → 得分：0.9
3. **标题包含关键词** → 得分：0.7-0.85
4. **分类精确匹配** → 得分：0.65
5. **分类包含关键词** → 得分：0.6
6. **内容包含关键词** → 得分：0.3-0.55

### 6.2 向量检索

使用 `BAAI/bge-large-zh-v1.5` 模型生成文本嵌入，通过Chroma向量数据库进行相似度检索。

### 6.3 结果融合

最终得分取关键词匹配和向量检索中的**最大值**，确保最佳匹配效果。

---

## 七、API接口参考

### 7.1 认证接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /auth/login | POST | 用户登录 |
| /auth/register | POST | 用户注册 |
| /auth/verify-token | GET | 验证Token |

### 7.2 文档接口

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| /documents | GET | doc_view | 获取文档列表 |
| /documents/{name} | GET | doc_view | 获取文档详情 |
| /upload | POST | doc_upload | 上传文档 |
| /documents/{name}/download | GET | doc_download | 下载文档 |
| /documents/{name}/share | GET | doc_view | 生成分享链接 |
| /documents/{name}/status | POST | doc_review | 更新文档状态 |
| /documents/{name}/category | POST | doc_edit | 更新文档分类 |
| /documents/{name}/review | POST | doc_review | 审核文档 |
| /documents/{name}/comments | GET/POST | doc_comment | 评论管理 |
| /documents/{name} | DELETE | doc_delete | 删除文档 |

### 7.3 检索接口

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| /search | GET | search | 智能检索 |
| /search/history | GET | history_view | 搜索历史 |
| /search/history | DELETE | history_manage | 清空搜索历史 |
| /search/hot | GET | - | 热门搜索 |

### 7.4 AI对话接口

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| /query | POST | chat | AI问答 |
| /query/knowledge-graph | POST | knowledge_graph | 知识图谱 |
| /query/recommendations | POST | recommend | 推荐文档 |

### 7.5 用户管理接口

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| /users | GET | user_view | 用户列表 |
| /users | POST | user_create | 创建用户 |
| /users/{id} | PUT | user_edit | 更新用户 |
| /users/{id} | DELETE | user_delete | 删除用户 |

### 7.6 角色管理接口

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| /roles | GET | - | 角色列表 |
| /roles | POST | role_create | 创建角色 |
| /roles/{id} | PUT | role_edit | 更新角色 |
| /roles/{id} | DELETE | role_delete | 删除角色 |
| /roles/{id}/status | POST | role_disable | 更新角色状态 |
| /roles/permissions | GET | - | 权限列表 |

### 7.7 系统管理接口

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| /health | GET | - | 健康检查 |
| /stats | GET | stats_view | 统计数据 |
| /llm/status | GET | - | LLM状态 |
| /llm/switch | POST | llm_switch | 切换模型 |

---

## 八、系统日志

系统自动记录以下操作日志：

| 操作类型 | 记录内容 |
|----------|----------|
| 文档上传 | 操作人、文档名称、片段数量 |
| 文档删除 | 操作人、文档名称 |
| 文档审核 | 操作人、文档名称、审核结果、审核意见 |
| 用户创建 | 操作人、用户名、角色 |
| 用户编辑 | 操作人、用户名、修改内容 |
| 用户删除 | 操作人、用户名 |
| 角色创建 | 操作人、角色名称 |
| 角色编辑 | 操作人、角色名称 |
| 角色删除 | 操作人、角色名称 |
| 角色禁用/启用 | 操作人、角色名称、状态 |
| AI查询 | 操作人、问题摘要、响应时间 |

---

## 九、常见问题

### Q1: 上传文档失败怎么办？

**可能原因：**
- 文件格式不支持
- 文件大小超过限制
- 网络连接问题

**解决方法：**
- 确认文件格式为PDF、Markdown、TXT、Word或Excel
- 检查文件大小，建议不超过50MB
- 检查网络连接，重试上传

### Q2: AI回答质量不佳怎么办？

**优化建议：**
- 确保问题描述清晰明确
- 上传相关文档到知识库
- 调整模型温度参数（降低温度提高回答确定性）
- 切换到云端模型获取更新的知识

### Q3: 搜索结果不匹配怎么办？

**改进方法：**
- 使用更精确的关键词
- 尝试不同的搜索词组合
- 检查文档是否已发布（仅已发布文档可被检索）
- 确认文档内容包含相关关键词

### Q4: 如何批量导入用户？

**操作步骤：**
1. 准备CSV格式文件，包含以下列：username, email, password, role, department
2. 在用户管理页面点击「导入用户」
3. 选择CSV文件并上传
4. 系统自动创建用户

### Q5: 分享链接无法访问？

**可能原因：**
- 链接已过期（24小时有效期）
- 文档已被删除

**解决方法：**
- 重新生成分享链接
- 确认文档仍存在且状态为「已发布」

---

## 十、注意事项

1. **数据安全**：敏感文档建议使用本地模型（Ollama），确保数据不出网
2. **权限管理**：定期审查用户权限，及时禁用离职用户账号
3. **文档审核**：所有上传文档需经过审核才能发布，确保内容质量
4. **系统备份**：定期备份 `chroma_db` 目录和 `data` 目录
5. **日志审计**：系统日志记录所有重要操作，便于追溯和审计

---

**版本**：v1.0  
**更新日期**：2026年7月  
**适用系统**：智识RAG文档问答中台